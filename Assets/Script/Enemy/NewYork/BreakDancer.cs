public class BreakDancer : Enemy
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //Damage Player if within range
    }

    public override void Die()
    {
        base.Die();
    }

}
